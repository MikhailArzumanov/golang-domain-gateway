# Golang domain gateway
Net service, that accepts connections with different domains. 

Concrete domain specifies target of proxying, that gateway offers as well.

## Configuring
See settings.json

## Build and run
### With building
`go build -o "%EXE_PATH%" && ./%EXE_PATH%`
### ..or plain run
`go run main.go`
